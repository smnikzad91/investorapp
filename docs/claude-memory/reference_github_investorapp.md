---
name: reference-github-investorapp
description: Where the tradeBot Investor Android app's git history lives
metadata:
  node_type: memory
  type: reference
  originSessionId: fbbbaf49-479a-4474-b5cd-7211814ba1fe
---

`D:\projects\android\bot` (see [[project_tradebot_investor]]) is pushed to
`https://github.com/smnikzad91/investorapp.git`, branch `main`. The user says this repo is
private. Remote `origin` and the local `main` branch (renamed from the git default `master`) are
already set up and tracking — `git push`/`git pull` work without extra setup.

Initial commit (2026-09-13) added everything except what `.gitignore` excludes; a follow-up commit
the same day removed `local.properties` from `.gitignore` and committed it too — see
[[feedback_local_properties_tracked]] for why, before re-ignoring it.
